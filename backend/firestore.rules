rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️  DEV MODE — all authenticated users can read/write everything.
    // TODO: restore per-collection rules before going to production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
